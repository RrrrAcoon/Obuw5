<Window x:Class="Obuw11.OknoZakazi"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:m="clr-namespace:Obuw11.Modeli"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Obuw11"
        mc:Ignorable="d"
        Background="#FFFFFF"
        FontFamily="Arial"
        WindowStartupLocation="CenterScreen"
        
        Title="Списко заказов" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Margin="10" Grid.Row="0" Orientation="Horizontal" x:Name="PanelPoiska">
            <Button Click="Nazad" Content="Назад"/>

            <Button x:Name="bthDobavit" Content="Добавить" Click="Dobavit" Margin="5"/>
            <Button x:Name="bthURedart" Content="редактировать" Click="Redakt" Margin="5"/>
            <Button x:Name="bthUdalit" Content="Удалить" Click="Udalit" Margin="5"/>

        </StackPanel>
        <ListView Margin="10" Grid.Row="1" x:Name="LvElement" HorizontalContentAlignment="Stretch" MouseDoubleClick="ClickElement">
            <ListView.ItemTemplate>
                <DataTemplate >
                    <StackPanel Margin="10"  Orientation="Horizontal" >
                        <StackPanel Margin="5" Width="550">
                            <TextBlock Text="{Binding Id,StringFormat='Артикул заказа: {0}'}" FontWeight="Bold"/>
                            <TextBlock Text="{Binding StatusZakaza.Nazvanie, StringFormat='Статус заказа:{0}'}"/>
                            <TextBlock Text="{Binding PunktVidachi.Adres, StringFormat='Адрес доставки заказа: {0}'}"/>
                            <TextBlock Text="{Binding DataZakaza, StringFormat='Дата заказа: {0}'}"/>
                        </StackPanel>
                        <TextBlock Text="{Binding DataVidachi, StringFormat='Датадоставки: {0}'}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>
